apiVersion: v1
kind: Service
metadata:
  name: expense-splitter
  labels:
    app: expense-splitter
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: expense-splitter
